import { Exclusion } from "../../utility/dist/index.js";

/**
 * Do something with an {@link Exclusion} parameter.
 *
 * This is based on something in the {@link https://www.gs1.org/genspecs | GS1 General Specifications}.
 */
export function doItWithExclusion(_exclusion: Exclusion): number {
    return 0;
}
